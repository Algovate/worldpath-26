# Optional future live-score provider configuration.
# Defaults to worldcup26 when unset.
# Supported values: worldcup26, mock, live
SCORE_PROVIDER=worldcup26

# Use only on the server. Do not expose real provider secrets with NEXT_PUBLIC_.
SPORTS_DATA_API_URL=
SPORTS_DATA_API_KEY=
